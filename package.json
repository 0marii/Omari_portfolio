{
  "name": "new-portfolio",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "start:python": "python3 -m http.server 3000",
    "build": "node scripts/update-sw.mjs && node scripts/generate-sitemap.mjs",
    "generate:sw": "node scripts/update-sw.mjs",
    "generate:sitemap": "node scripts/generate-sitemap.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "build:catalog": "node scripts/build-catalog.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "serve": "^14.2.6",
    "vitest": "^3.1.4"
  }
}
